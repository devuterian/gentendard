# Gentendard Plans

- Add optional CI job that runs `make build` on tags only (cache vendor zips).
- Subset or split webfont packages for smaller downloads.
- Document Unicode ranges overlaid from Pretendard in machine-readable form (JSON) for audits.
- Gather user feedback on CJK/Hanja behavior for a possible v0.2 policy tweak.
